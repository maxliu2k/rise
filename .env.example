# Copy to .env and adjust as needed.

# Where the audio + pipeline artifacts live (manifest.csv, work/, features/, pipeline/).
# Defaults to <repo>/all-samples if unset. Point this elsewhere to run the same code on a
# different data location (e.g. an external drive or the IRMAS set).
# RISE_DATA_ROOT=/Users/gavin/Music Project/Real Project/rise/all-samples
